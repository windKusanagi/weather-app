const initialState = {
	citySearchStr: "null",
	citiesAutocomplete: [],
};

const searchReducer = (state = initialState, action) => {
	switch (action.type) {
		
		default:
			return state;
	}
};

export default searchReducer;
